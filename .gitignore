node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts

# Supabase CLI local cache
supabase/.temp

# Local AI-assistant instructions (not part of the site)
CLAUDE.md
AGENTS.md

# One-time passwords exported for handing out (never commit)
.akun/

# Raw photo dumps; the cropped copies are what ship
src/assets/tentor/*.zip
